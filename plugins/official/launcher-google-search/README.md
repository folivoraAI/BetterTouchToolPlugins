# Google Search Launcher

Launcher plugin example that turns the current launcher query into a Google
search URL and opens it in the default browser.

## Installation

Drop `GoogleSearchLauncher.swift` onto the BetterTouchTool preferences window.

## Safety Notes

This plugin opens `https://www.google.com/search` in the default browser. The
search query is sent to Google by the browser after the URL is opened.
