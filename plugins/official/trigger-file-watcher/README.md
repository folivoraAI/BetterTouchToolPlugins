# File Watcher

Trigger plugin example that watches a configured file or folder using a dispatch
source and fires a BetterTouchTool trigger when the target changes.

Install by dropping `FileWatcherTrigger.swift` onto the BetterTouchTool
preferences window.
