# Launcher Pong

Launcher plugin example that hosts a small Pong game in a native launcher
surface. It demonstrates SwiftUI rendering, live updates, and launcher surface
interaction.

## Installation

Drop `LauncherPong.swift` onto the BetterTouchTool preferences window.

## Safety Notes

This plugin is self-contained and does not access files, the network, the
clipboard, or shell commands.
