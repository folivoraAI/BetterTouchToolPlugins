# Clipboard Change

Trigger plugin example that watches the system pasteboard and fires a
BetterTouchTool trigger whenever the clipboard changes.

Install by dropping `ClipboardChangeTrigger.swift` onto the BetterTouchTool
preferences window.
