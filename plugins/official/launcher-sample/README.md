# Sample Launcher Plugin

Demonstrates the main launcher plugin APIs: top-level results, child rows,
commands with shortcuts, BTT variables, named trigger execution, and a native
launcher surface.

Install by dropping `SampleLauncherPlugin.swift` onto the BetterTouchTool
preferences window.
